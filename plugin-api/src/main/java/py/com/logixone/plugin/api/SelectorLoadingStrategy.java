package py.com.logixone.plugin.api;

/** Bounded strategy used to obtain selector options without defining visual behavior. */
public enum SelectorLoadingStrategy {
    INLINE,
    SEARCH_ON_DEMAND
}
