package py.com.logixone.plugin.api;

/** Governance classification for the values offered by a selector. */
public enum SelectorSourceKind {
    CLOSED_STATE,
    BUSINESS_CATALOG,
    OPERATIONAL_REFERENCE,
    NORMATIVE_CATALOG,
    DEPLOYMENT_COMPOSITION
}
