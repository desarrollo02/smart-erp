package py.com.logixone.plugin.api;

/** Semantic role of a region; it never selects markup or a CSS class. */
public enum ScreenRegionRole {
    CONTEXT,
    FILTERS,
    WORK_ITEMS,
    HEADER,
    LINES,
    SUMMARY,
    GUIDANCE,
    CONTENT,
    ACTIONS
}
