package py.com.logixone.plugin.api;

/** Closed semantic intent of an action, independent from its visual widget. */
public enum ScreenActionIntent {
    NAVIGATE,
    SEARCH,
    CREATE,
    EDIT,
    ADD_LINE,
    REMOVE_LINE,
    SAVE_DRAFT,
    SUBMIT,
    APPROVE,
    REJECT,
    CONFIRM,
    EXECUTE,
    CANCEL,
    CLOSE
}
