package py.com.logixone.plugin.api;

/** Operations exposed by the authorized administration route of a selector source. */
public enum SelectorManagementCapability {
    VIEW,
    CREATE,
    EDIT,
    INACTIVATE,
    UPDATE_VERSION
}
