package py.com.logixone.kernel.domain.security.system;

/** Stable fail-closed diagnostics for kernel-wide authorization. */
public enum SystemSecurityDiagnosticCode {
    USER_INACTIVE,
    SYSTEM_ROLE_CONTEXT_INVALID
}
