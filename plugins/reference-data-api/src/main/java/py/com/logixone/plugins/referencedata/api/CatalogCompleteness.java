package py.com.logixone.plugins.referencedata.api;

/** Prevents a characterized subset from being represented as a full publication. */
public enum CatalogCompleteness {
    BOOTSTRAP_SUBSET,
    FULL
}
