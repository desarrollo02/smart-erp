package py.com.logixone.kernel.application.audit.admin;

/** Closed categories produced by kernel audit ports. */
public enum AuditEventCategory {
    COMPANY_OPERATION,
    SECURITY_OPERATION,
    TRUSTED_ACCESS,
    SYSTEM_AUTHORITY_OPERATION,
    SYSTEM_AUTHORITY_ACCESS,
    PLUGIN_OPERATION
}
