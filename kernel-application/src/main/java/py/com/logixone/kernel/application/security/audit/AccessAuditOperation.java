package py.com.logixone.kernel.application.security.audit;

public enum AccessAuditOperation {
    RESOLVE_COMPANY_CONTEXT,
    SELECT_COMPANY,
    RESOLVE_NAVIGATION,
    RESOLVE_SCREEN,
    AUTHORIZE_PLUGIN_OPERATION
}
