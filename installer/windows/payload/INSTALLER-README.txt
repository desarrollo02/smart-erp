LOGIXONE 0.9.0-INTERNAL.1 - WINDOWS

1. Keep every file in this directory together.
2. Double-click Logixone-Setup-0.9.0-internal.1.exe.
3. The first analysis is read-only and does not request UAC.
4. Review every warning and the seven planned phases.
5. Read the Docker Desktop license if Docker must be installed.
6. Consent only when routes, ports, downloads and actions are correct.
7. Never use docker compose down --volumes for normal shutdown.

Diagnostic CLI:
Logixone-Installer.Cli.exe --preflight --json
Logixone-Installer.Cli.exe --preflight --plan

This edition is INTERNAL_UNSIGNED. It is not approved for delivery to a company.
